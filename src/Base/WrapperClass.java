package Base;

public class WrapperClass {
    public static void main(String[] args) {
        // Reference types are heavier for the processor, so use them wisely!

        String word = "Hello";
        Integer numb = 30;
        Character char1 = 'c';
        Double doub = 14.4;
        Boolean bool = false;

        // Why use references? Because ArrayLists (an evolution of arrays) accept ONLY reference types.



    }
}
